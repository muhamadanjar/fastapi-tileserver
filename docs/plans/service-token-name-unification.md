# Service Token Name Unification Plan

Adopt `UPLOAD_API_SERVICE_TOKEN` as the canonical Tileserver credential name for calls to Upload API, retaining the former caller name as a compatibility alias.

Related Progress: [Service Token Name Unification Progress](../progress/service-token-name-unification.md)
