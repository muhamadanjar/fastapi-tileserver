# Upload Artifact Handoff Plan

Related Progress: [Upload Artifact Handoff Progress](../progress/upload-artifact-handoff.md)

Accept available `upload_api` artifacts as tiling sources while preserving every legacy upload session, endpoint, and stored path. Artifact-backed jobs acquire a lease and use an ephemeral working copy before invoking existing geospatial validation and processing.

