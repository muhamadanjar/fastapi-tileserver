# Database
DB_TYPE=postgresql
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=gis_db

# RabbitMQ
RABBITMQ_URL=amqp://guest:guest@localhost:5672/

# Redis
REDIS_URL=redis://localhost:6379/0

# Upload
CHUNK_UPLOAD_THRESHOLD=10485760

# CORS
CORS_ALLOWED_ORIGINS=http://localhost,http://localhost:3000,http://localhost:3001,http://localhost:8070
CORS_ALLOWED_CREDENTIALS=True
CORS_ALLOWED_METHODS=GET,POST,PUT,DELETE,PATCH,OPTIONS
CORS_ALLOWED_HEADERS=Content-Type,Authorization,Accept,X-Requested-With,Content-Range

GEOSERVER_USER=admin
GEOSERVER_PASSWORD=geoserver
GEOSERVER_URL=http://localhost:8080/geoserver
GEOSERVER_WORKSPACE=tileserver
UPLOAD_API_URL=http://localhost:8010/api/v1
# Token that Tileserver sends to Upload API. It must equal the "tileserver" value
# in Upload API's UPLOAD_API_TRUSTED_SERVICE_TOKENS JSON map.
UPLOAD_API_CALLER_TOKEN=replace-with-tileserver-upload-api-caller-token
# Deprecated fallback: UPLOAD_API_SERVICE_TOKEN=
